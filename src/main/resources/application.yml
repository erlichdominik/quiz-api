spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:db
    username: sa
    password: password
    driver-class-name: org.h2.Driver

app:
  jwt:
    secret: abcdefghijklmnOPQRSTUVWXYZ